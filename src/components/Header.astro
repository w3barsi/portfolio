---
const { current } = Astro.props;

export enum Nav {
  HOME,
  BLOG,
}
---

<header
  class="pt-5 flex text-c-fg gap-10 text-lg justify-center items-center my-5 bg-gray font-mono"
>
  <div class={`cursor-pointer ${current === Nav.HOME ? 'text-c-blue' : ''}`}>
    Home
  </div>
  
</header>

<style>

</style>
